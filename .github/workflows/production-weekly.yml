name: Testops Report WEEKLY 

# Sat. @4am UTC
on:
  schedule:
    - cron:  "0 4 * * 6" 
  workflow_dispatch:

jobs:
  deploy:
    name: Testops Report WEEKLY
    uses: ./.github/workflows/all-jobs.yml
    with:
      database: 'production'
    secrets: inherit